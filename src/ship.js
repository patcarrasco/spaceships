class Ship {
  constructor({width, height, color, x, y}) {
    this.width = width
    this.height = height
    this.color = color
    this.x = x
    this.y = y
  }

  render() {
    let ctx = canvas.getContext("2d");
  }
}